<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.kanozo.mappers.ProductMapper">
	<select id="productRetrieve" resultType="ProductVO">
		SELECT * FROM product WHERE productType = #{productType}
	</select>
	
	<select id="productGetDetail" resultType="ProductVO">
		select * from product WHERE productId = #{id}
	</select>
	
	<insert id="addBasket">
		INSERT INTO basket(userId, productId, numbers, validity) values(#{userId}, #{productId}, #{numbers}, 1)
	</insert>
	
	<update id="productUpdate">
		UPDATE product SET productType = #{producttype} productName = #{productname} explanation = #{explanation} price = #{price} inventory = #{inventory} WHERE productId = #{productid}
	</update>
</mapper>
 